import { MedusaContainer } from '@medusajs/framework/types'; import { ContainerRegistrationKeys, Modules } from '@medusajs/framework/utils'; export default async function fixSalesChannels({ container }: { container: MedusaContainer }) { const query = container.resolve(ContainerRegistrationKeys.QUERY); const remoteLink = container.resolve(ContainerRegistrationKeys.REMOTE_LINK); const { data: salesChannels } = await query.graph({ entity: 'sales_channel', fields: ['id'] }); const { data: apiKeys } = await query.graph({ entity: 'api_key', fields: ['id'] }); console.log('Linking API Keys to Sales Channels...'); for (const ak of apiKeys) { for (const sc of salesChannels) { try { await remoteLink.create({ [Modules.API_KEY]: { api_key_id: ak.id }, [Modules.SALES_CHANNEL]: { sales_channel_id: sc.id } }); console.log('Linked API Key ' + ak.id + ' to Sales Channel ' + sc.id); } catch(e) {} } } console.log('Done linking.'); }
